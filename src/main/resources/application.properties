spring.application.name=delivery-api
spring.datasource.url=jdbc:mysql://localhost:3306/delivery-api?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=secret123

#spring.jpa.generate-ddl=true
#spring.jpa.hibernate.ddl-auto=create

#faz com que o flyway vai nesses dois diret�rios, isso � apenas para teste em dev
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect

#n�o deixa mandar json com propriedades que n�o existem ou que est�o com jsonignore
spring.jackson.deserialization.fail-on-unknown-properties=true
spring.jackson.deserialization.fail-on-ignored-properties=true

#lan�a exce��o se n�o tiver um handler (um codigo mapeado, um recurso mapeado)
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false